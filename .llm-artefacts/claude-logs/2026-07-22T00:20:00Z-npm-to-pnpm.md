# Replace npm with pnpm in install script & fork docs

Started: 2026-07-22T00:20:00Z

## Goal

User asked to replace npm with pnpm. Scope: the commands the user types —
`install.local.sh` and the instructions in `README.local.md`. The `npm run`
calls inside package.json's own scripts are left as-is (they work fine when
invoked via pnpm, and touching them would grow the upstream diff).

## Steps

- 2026-07-22T00:20:00Z — Created log.
- install.local.sh: `npm install` → `pnpm install`, `npm run build:pacman` →
  `pnpm run build:pacman`. Re-ran shellcheck: clean.
- README.local.md: prerequisites now Node.js + pnpm; command block updated.
- Note: repo has package-lock.json (npm); pnpm ignores it and creates
  pnpm-lock.yaml on first install.
- Left everything unstaged per user rules.

## Follow-up: eza alias vs `ls -t`

- User's interactive `ls` is eza; `ls -t` errors there. In the script (non-interactive
  bash) `ls` resolves to /usr/bin/ls, so it worked — but user asked to make it robust.
- First attempt (pure-bash `-nt` loop) rejected as too complex.
- Final: `/usr/bin/ls --sort=time ... | head --lines=1` — absolute path dodges any
  alias/shim, `--sort=time` is the long form of `-t` (newest first, verified).
- shellcheck: clean. Recorded preference in user-decisions.md.
